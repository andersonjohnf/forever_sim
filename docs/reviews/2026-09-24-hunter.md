# Hunter, H2 (2026-09-24)

Marksmanship, Beast Mastery and Survival on the ranged and pet core, landed under D27's 90/10 mode:
Aimed Shot, Multi-Shot, Arcane Shot, Serpent Sting, Sniper Shot, Hunter's Mark, Rapid Fire and
Bestial Wrath with Forever's changes, the talents that touch them, a cat for Beast Mastery and
Survival, Lone Wolf for Marksmanship, mana, ammo and quivers in their own Gear slots, and the
ranged sheet. Documented in `docs/classes/hunter.md`. First pass (3,000 fights, seed 1): MM 501.4,
BM 485.2, SV 420.8.

## Combined review (D27)

The reviewer checked the constants against the client and hunter.md §3–§4 (Aimed Shot, Multi-Shot's
shared category, Sniper Shot, Rapid Fire, Bestial Wrath and the talent curves), the worked examples
WE-H1–H8, that the Season of Discovery rows reach no engine code, that no talent reaches the pet
twice, mana over 180 s and 600 s, the edge cases (no ranged weapon, no ammo, no quiver, arrows in a
gun, a 9% hit sheet), the racials, determinism, and screenshots of every hunter screen at 390 and
1280 px, light and dark.

**Blocking:** HN1 and HN2 (medium, introduced). No high findings.

| # | Severity | Origin | Finding | Disposition |
| --- | --- | --- | --- | --- |
| HN1 | medium | introduced | A Skyborne hunter's run said "Hunter simulation isn't available yet": only the warrior got the Skyborne sentence. | fixed, `67f02f4`: the refusal names the class ("…a Skyborne hunter can't be simulated. Pick another race."); message only, the blocker fires as before; test |
| HN2 | medium | introduced | The Ammo picker offered arrows to a gun with no mark, led by Swiftfeather Arrow (+24.6 DPS, adds 0), and a new ranged weapon kept the old ammo. | fixed, `9018a70`: ammo the weapon doesn't fire is dimmed, sorted last and says why ("For bows and crossbows: your gun fires bullets"), in the picker and its slot row; a weapon of the other kind swaps the ammo, and the quiver or pouch at the same haste (`matchSupplies`); known gap removed; unit and 390 px e2e tests |
| HN3 | low | introduced | The Rotation tab's fixed Trueshot Aura and Pet rows read the same on every build. | known gap (`c789bd6`): wording only, the runs are right |
| HN4 | low | introduced | Forever's Rapid Recuperation and Resourcefulness (mana while casting) are neither simulated nor named in hunter.md §4. | known gap (`c789bd6`): the 180 s defaults don't change; they matter in long fights |
| HN5 | low | introduced | Survival's default spends 5 points in Improved Arcane Shot with Arcane Shot off; IS3/RK2/IAS1 sims 2.0% higher. | known gap (`c789bd6`): within D27's ±5%; D23's tuning picks the build |
| HN6 | low | introduced | Survival is ranged-only, and the `hunterNoMelee` note names only Raptor Strike. | known gap (`c789bd6`): hunter.md and the milestones say so |
| HN7 | low | introduced | `arcaneShotResists`, which covers Serpent Sting too, showed only with Arcane Shot. | fixed, `539f067`: listed with either shot; test |
| HN8 | low | introduced | "Wait for Auto Shot" isn't dimmed while Shared cooldown is "Neither". | known gap (`c789bd6`): it does nothing then and costs nothing |
| HN9 | low | introduced | Rapid Fire's help said "3 with Rapid Killing" (1 point gives 4); Serpent Sting's said 555 only. | fixed, `539f067`: "4 with 1 point in Rapid Killing, 3 with 2"; "(666 with Improved Stings 3/3)" |
| HN10 | low | introduced | Battle Shout stays on, unmarked, for a Lone Wolf hunter with no pet. | known gap (`c789bd6`): needs the talents in `buffUnusedReason` |
| HN11 | low | introduced | hunter.md §7.3 called the default ammo "the best the pool has outside raids", though Miniature Cannon Balls and Doomshot are better. | fixed, `539f067`: the best non-epic ammo with a Forever row, and why the others aren't |
| HN12 | low | introduced | The ranged sheet shows no spell crit, which Serpent Sting's ticks use. | known gap (`c789bd6`): hunter.md OQ-H1 |
| HN13 | low | pre-existing | Blackhand's Breadth's use and Dwarven Hand Cannon's chance on hit aren't simulated (about 0.6%). | known gap (`c789bd6`): flagged in the assumptions, as other items' effects are |

Only HN1 touched `src/sim/plan`, and only the blocker's text; HN2's `matchSupplies` is a Gear-tab
helper in `src/sim/defaults.ts` that `defaultGear` shares (its picks are unchanged), and HN7 adds an
assumption to the list. No fix changed engine logic, so D27 asks no verification pass; the goldens
didn't move.

## Rebase onto main (`f44d4e9`)

One conflict, in `docs/milestones.md` (H1 ticked on main, H2's line on the branch): both kept. Main's
phone bar is now named "Show results and details"; the hunter e2e matches "Show results" as a
substring, so it needed no change.

Branch after the fixes: lint ✓ · typecheck ✓ · unit ✓ (2382) · e2e ✓ (363). Two earlier runs failed
only the Retribution benchmark (4,544 and 4,863 fights a second against a 5,000 floor) under a machine
load of about 7; alone it runs about 8,000 a second on both main and the branch.

## Verification pass (with the phone bar's, on main `1750dbb`)

| id | sev | origin | finding | disposition |
| --- | --- | --- | --- | --- |
| VF3 | medium | introduced by HN2 | A new weapon of the other kind swapped the ammo to the default, not to the other kind's ammo of the same damage: Swiftfeather Arrow (24.617) became Thorium Shells (17.715), so a bow against a gun looked about 3.4% worse from the ammo alone. | fixed: `matchSupplies` picks the other kind's ammo with the closest damage per second (its twin when there is one; the default on a tie), Doomshot → Miniature Cannon Balls; unit test; hunter.md §7.3 |
| VF4 | low | introduced by the rebase | This log cited the pre-rebase commits. | fixed: the hashes are main's |
| VF5 | low | introduced by the merge | milestones.md: K3's "P1 below" pointer, H1 and K3's stale "awaiting review", M5.7 before M5.6. | fixed; the H3 line comes with the Demonology branch |
| VF6 | low | introduced by HN2 | No test checked the swap's announcement. | fixed: the hunter e2e asserts the live region's text |

The rest of the pass (the phone bar's VF1, VF2 and VF7) is in
[2026-09-24-phone-details.md](2026-09-24-phone-details.md). Since VF3's fix is one function with
its own unit tests, it gets no further pass (step 5: a low's or a small fix's quick check is its
tests and the lead's run).

## Verdict

**Merged** onto main after the lead's merge check (D25); the verification pass's findings are fixed.
