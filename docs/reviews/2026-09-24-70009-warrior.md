# The warrior on build 1.60.1.70009 (2026-09-24)

The warrior slice for build 1.60.1.70009: Sunder Armor's threat 206 + 0.05 × AP, Shield Slam's
"very high" +475, Slam's 18 s cooldown, Bloodthrill's main-hand proc into the dodge's window, the
Overpower window that no longer stacks, and Max TPS keeping Shield Block. It had a combined logic
and UX review (D27's 90/10 mode) by a fresh reviewer who wrote none of it, on branch
`worktree-agent-aac9382a9913f9724` (`d03014e0`, `62fca3d9`). This log records the review and the
fix round that answered it.

The slice was merged onto the 70009 branch (`build-70009`: main, the 1.60.1.70009 data and the
talent migration) at `9712199d` with no conflicts. The merge left `spells.json` one spell short of
what the generator writes: warrior.md now cites the old Bloodthrill window spell 1289681, and the
docs' interest set takes every cited id. `6f9f6822` regenerates it from the cached 1.60.1.70009
client (no requests); `npm run scrape:check -- --if-cached` then passes. No snapshot or golden
moved in the merge.

Probes: `.cache/probes/70009-warrior-review/` in the slice's worktree (the reviewer's); the fix
round reran `ap.probe.ts` and `ss.probe.ts` on this branch, plus a Lacerate AP probe, in
`.cache/probes/wr/`. Screenshots of the changed Rotation tab (Max TPS picked) at 1280 px light and
390 px dark: `.cache/snaps/wr-maxtps-*.png`.

The defaults on seed 31101 (100,000 paired fights), unchanged by the fix round: Defensive 926.27
TPS, Balanced 993.37, Max TPS 1,003.49.

## Combined review (WR)

| # | Severity | Origin | Finding | Disposition |
| --- | --- | --- | --- | --- |
| WR-1 | medium | introduced | With Shield Block kept, Max TPS shares Balanced's rows, and its line and help (only against Defensive: +8% TPS, 21% more damage taken) read the same as Balanced's (+7%, 21%). | fixed in `c9f871f1`: the line reads "Sunder Armor filler from its cost, Heroic Strike from 45 rage: about +1% TPS over Balanced for the same damage taken." (117 characters), and the help names both thresholds against Balanced's and gives both comparisons: against Balanced 1.0% more TPS, 0.5% more DPS and the same damage taken (+10.12 TPS, +9.90 to +10.33; +1.89 DPS; +0.40 damage taken a second, +0.05%), against Defensive as before. warrior.md §5.4 records it; the e2e checks the line and the help |
| WR-2 | low | introduced | The 0.05 × AP default was justified as the share that makes 261 "at 1,100 AP, about a level-60 tank's before raid buffs", but the default tank has 802 without raid buffs, 1,241 with them, and 1,494 on average when its Sunders land. | fixed in `b36501f5`: the share stays 0.05, stated as a reasoned guess that keeps the total near Classic Era's 261: 261 at 1,100 AP (a round figure between 802 and 1,241), about 281 at the default tank's ~1,500 AP when its Sunders land (+8%). The same in threat.md (the Sunder row and the wording table), warrior.md §1 and Q1, open-questions B10, the result's assumption and `abilities.ts`. The wording table's Lacerate note (and threat OQ 4's, which said the same) now uses the bear's AP when its Lacerates land, ~1,360 (1,356 over 300 fights), so about 274; Lacerate's value is the druid slice's and is unchanged |
| WR-3 | low | introduced | threat.md's wording table had the Heroic Strike "on the next swing" row twice. | fixed in `b36501f5`: the second copy is gone |
| WR-4 | low | introduced | D26 still said, in the present, that dropping Shield Slam gains 3.3% TPS at +254, which held only before 1.60.1.70009. | fixed in `c422480a`: past tense, and the build's number: dropping it costs Max TPS 12.85% of its TPS even at +254 (957.77 → 834.72, seed 31101, 6,000 fights), so the untested value no longer decides it |
| WR-5 | low | introduced | The `whiteThreat` assumption said Sunder Armor and Shield Slam "follow Forever's values", but Shield Slam's +475 and Sunder's AP share aren't Forever values. | fixed in `55e640d9`: "use their own values (below)", and "uses its own value" for Sunder Armor alone (a new unit check with Shield Slam off) |
| WR-6 | low | introduced | `SHIELD_SLAM_THREAT` cited warrior.md Q1 and open-questions B13 for its test. | fixed in `55e640d9`: Q34 and C6 |
| WR-7 | low | introduced | The no-AP-coefficient checks (`client.test.ts`'s Sunder test, claim D4) read only the spell power coefficient. | fixed in `55e640d9`: both also assert the attack power coefficient is 0. The column is `BonusCoefficientFromAP`, which the scraper writes as `bonusCoefficientFromAp` (its `camel()`), so that's the key read; `bonusCoefficientFromAP` would always be undefined. The claims report from the cache reads "from AP 0/0/0/0/0" |
| WR-8 | low | introduced | Bloodthrill's `ProcTypeMask` gained a second word, 0x20, in 1.60.1.70009, and nothing recorded it. | fixed in `55e640d9`: warrior.md §2.8 and Q11 record it as [?] (no source says what the bit means; the notes describe main-hand melee attacks only, so the sim adds no trigger for it), with a test to run in game; `abilities.test.ts` pins the word so a change shows |
| WR-9 | medium | pre-existing | The bear's default makes 1,116 TPS, above the officers' 800–900, and now 12% above the warrior's Balanced (993). | confirmed tracked: the milestones' T6 already holds it as TI-2's open plausibility finding for the druid slice, with its in-game tests; `3982fcdc` adds the 1.60.1.70009 gap (the bear 1,116, the paladin 829, seed 31101, 20,000 fights, both before their own 1.60.1.70009 slices) |
| WR-10 | low | introduced | Shield Slam's +475 moves the headline, and the docs didn't say by how much. | fixed in `c422480a`: Q34, C6, threat OQ 1 and warrior.md §5.4: at +254 the default Balanced makes 946.78 TPS, −4.7% against +475's 993.82, and Max TPS 957.77 (−4.6%); dropping Shield Slam at +254 costs Balanced 13.37% and Max TPS 12.85% (seed 31101, 6,000 fights) |
| WR-11 | low | introduced | The Rotation tab's intro said Defensive and Max TPS "are tuned for the default setup", but on 1.60.1.70009 they had only a first-pass check (D27). | fixed in `5748f32a`: "Defensive and Max TPS were tuned on an earlier game build and had a quick check on this one; Balanced, the default, hasn't been fully tuned yet." In players' words, as ux.md asks; ux.md and the e2e quote it |

## Checks

- `npm run lint` and `npm run typecheck`: clean.
- `npm test`: every warrior, plan, APL and client test passes. The failures left are the ones
  `build-70009` has on its own (the other classes' 1.60.1.70009 slices: paladin, druid, shaman,
  Eureka!, Righteous Fury); the merge fixed the five warrior ones it had. The Retribution benchmark
  and one `sim-store` test fail only under load and pass alone.
- E2E (port 4391): `protection-rotation`, `tank-results`, `rotation-tab`, `priority-list` and
  `app`, 79 passed.
- `npm run scrape:check -- --if-cached`: every dataset matches a fresh generation.

The fix round changed copy, docs and tests; no engine logic moved (the defaults' numbers above are
unchanged), so by D27 it needs no verification pass.
