# Infrastructure hardening: GitHub issues #7, #8 and #9 (2026-09-24)

The infra slice (`9a82c90a`..`ab9a3561`): doc citations checked against their headings, the
deploy's Pages and OIDC permissions on the deploy job only, the scrapers checking every redirect
hop and validating build versions and cache keys, shadcn as a dev dependency, a worker-pool
watchdog, and a Content-Security-Policy. A fresh reviewer who wrote none of it did the combined
logic and UX review; the fix round below answers it.

Nothing high; OR-1 (medium, introduced) blocked the push.

| # | Severity | Origin | Finding | Disposition |
| --- | --- | --- | --- | --- |
| OR-1 | medium | introduced | The watchdog's 60 s `setTimeout` kept counting while a phone or Energy Saver froze the tab, and on waking fired before the worker could answer: a healthy run failed. Reproduced by suspending only the reviewer's own renderer for 65 s during a 100,000-fight Protection paladin run. | fixed in `6afbff73`: a 1 s heartbeat runs while the pool has work; each beat adds at most 2 s to a busy worker's silence, any answer resets it, hidden time doesn't count, and a worker fails at 60 s of awake silence. Unit tests (fake timers): a 65 s suspension mid-chunk doesn't fail it, 60 s of awake silence around one still does, hidden time doesn't count, and a chunk queued between beats gets its full timeout. An e2e test drives a silent worker to the error through the page clock. architecture.md follows |
| OR-2 | low | introduced | The hang's message talked about the app ("A simulation worker stopped responding (no answer in 60 s)"), against ux.md's checklist item 2, and was followed by the reset-to-defaults advice, which can't help a hang. | fixed in `6afbff73`: "The simulation stopped responding for a minute, so it was stopped. Run it again.", with no retry advice after it (`carriesItsOwnAdvice`, unit-tested); ux.md's Error state says so. Screens checked at 390 and 1280 px, light and dark |
| OR-3 | low | introduced | `fetchAllowed` fired redirect hops back to back, without the 1.1 s gap; `requests.jsonl` got one line per chain, and a refused hop threw before the request already made was logged. | fixed in `168cae21`: `onRequest`/`onResponse` hooks in the hop loop; the fetcher spaces and logs each hop there. Fake-fetch tests: a three-hop chain is spaced ≥ 1.1 s and logged hop by hop, and a chain refused at its second hop logs the first. client.md follows |
| OR-4 | low | introduced | architecture.md's CSP table didn't say `worker-src` governs loading the worker, not what it does. | fixed in `da7f97d1`: a same-origin dedicated worker takes its policy from its own response headers, which Pages doesn't send; it fetches nothing today |
| OR-5 | low | pre-existing | `docs/data/items.md:416` linked to `client.md#what-m15c-2-needs-to-know`; the heading's anchor is `#what-m15c-2-needed-to-know-and-what-it-did`. | fixed in `195b3abc`, and the anchor test now checks the docs' own relative and in-doc anchored links (about 2,200); that was the only broken one |
| OR-6 | low | introduced | `e2e/csp.spec.ts` checked Geist with `document.fonts.check`, which passes when no face is needed. | fixed in `da7f97d1`: after `document.fonts.ready`, Geist Variable and Josefin Sans each have a `FontFace` with status `loaded` |
| OR-7 | info | n/a | (a) A meta CSP can't carry `frame-ancestors`, so the page can be framed; Pages sends no headers to fix that. (b) The optimizer's future pool work units must answer within 60 s of awake time, or the timeout must scale with them. (c) The lockfile's oxide-wasm entries predate this slice. | noted; no code. (a) is in architecture.md's CSP section; (b) is in its Workers paragraph (`6afbff73`) |

Checks after the fix round: lint, typecheck, `npm test` (110 files), and the csp, app,
results-states and results-keyed e2e specs (51 tests), all green.

Verdict: pending a verification pass by a fresh reviewer, scoped to `6afbff73`..`da7f97d1` (OR-1 changed the run layer).
