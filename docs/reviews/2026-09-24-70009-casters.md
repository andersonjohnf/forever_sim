# The 70009 casters slice (2026-09-24)

The casters slice (`worktree-agent-ac9524913a85d0c7e`) adopts 1.60.1.70009 for the mage, shaman,
Balance, Eureka! at 10%, Wizard Oil 24, the Frenzy potions and Dark Sacrifice. It's merged onto
`build-70009` (main, the 70009 data and the talent migration) in `f378037e`. The merge resolved
four conflicts:

- the Eureka! help texts: main's "the next 3 of the spells (attacks) it covers" with the slice's 10%
- the Arcane golden: re-taken on the merge, and its history names both EM-6's Mindfang and 70009's
  Ignite, 435.43 → 435.33 DPS
- `docs/data/client.md`: both sides' text kept
- the generated data: `scrape:check` matches, 0 requests

A fresh reviewer did the combined logic and UX review (D27's 90/10 mode). Its findings and their
dispositions follow. Nothing was high. MC-1 and MC-3 (medium) blocked the push, and MC-2 is owned
by another slice.

| # | Severity | Origin | Finding | Disposition |
| --- | --- | --- | --- | --- |
| MC-1 | medium | introduced | The warrior's Eureka! still said 40%: the racial's help text in `warrior/shared.ts`, and in warrior.md the §2.3 costs (Bloodthirst and Mortal Strike 30 → 18, Whirlwind 25 → 15, Execute 15 → 9), the §2.9 racials row and Q18. The engine uses 10%, rounded down. | fixed in `10e2c4d8`: the help text says 10% less rage. warrior.md gives 30 → 27, 25 → 22 and 15 → 13 (10% off, rounded down, as `eureka.ts` pays). The racials row says 10% and names the old 40%, and Q18 asks how 10% of 15 (1.5) rounds |
| MC-2 | medium | pre-existing | Thorns and Retribution Aura scale with spell power. | owned by the paladin slice (`effects/buffs.ts`, `paladin/protection.ts`). Not changed here |
| MC-3 | medium | introduced | The Frenzy potions (+80, +56 and +40 attack power and ranged attack power, auras 99 and 124) weren't in the catalogue. The docs called them "a known gap", and the milestones had no entry. | fixed in `4efecf2a`: the Major Frenzy Potion is a catalogue entry in the potion group (`cooldown:potion`, 120 s through its spell's category). It's for the specs that attack (`forSpecs: 'melee'`, which hunters see). Every rotation drinks it on cooldown from the pull (`classes/shared-consumables.ts`) [?]. D29, measured at Max consumables against each spec's current potion (seed 12345, 20,000 fights for the close cases): the rogues gain +1.0–1.2% (they drank no potion), Enhancement +0.9%, Marksmanship +0.9% and Survival +0.6% (each over the Major Mana Potion), so Max gives them the Frenzy potion. The others keep their potion: Beast Mastery (−3.5%), Fury (−1.1%), Arms (−1.7%), Retribution (−1.1%), the tanks (−0.7% to −2.8% TPS) and the cat (+0.2%, a tie with the Mighty Rage its rotation times). The Standard raid is unchanged, so no golden moves. The Superior and Greater potions aren't catalogued: they're lower ranks of the same potion |
| MC-4 | low | introduced | mage.md's Ignite "no double dip" paragraph said no other spell carries `Attributes[6]` 0x20000000. In fact 119 do. | fixed in `d3724139`, read from the cached SpellMisc of both builds: 412545 is the only spell that gained the flag in 70009, and it gained `Attributes[10]` 0x2 with Deep Wound's tick 412613 alone (17 spells carry that one) |
| MC-5 | low | pre-existing | Deep Wound's tick spell 412613 carries the same flags, "ignore caster damage modifiers" among them. Yet warrior.md §2.5 applies Death Wish, Enrage and Two-Handed Weapon Specialization to every tick. | recorded in `03eaf890` as warrior.md Q36, with the flag evidence (0x20000000 since 69913, 0x2 new in 70009) and an in-game test. §2.5 points to it. The engine is unchanged, as briefed |
| MC-6 | low | introduced | 70009 added `Attributes[11]` 0x400 to Flametongue Totem and to the lower Windfury and Grace of Air ranks as well, and the notes say Flametongue no longer stacks with Windfury. The docs named only 10612, 25360 and 25909. | fixed in `aeb02997`: shaman.md#totems, client.md and the buffs doc's `totem:air` row list all 11 flagged auras (Windfury 8515, 10609, 10612; Grace of Air 8836, 10626, 25360; Flametongue 8230, 8250, 10521, 15036; Tranquil Air 25909). The new citations bring them and 412613 into `spells.json` (9 spells, regenerated from the cache, 0 requests). The client-values test checks all 11 by name. The sim still models only the rank-3 totems |
| MC-7 | low | introduced | client.md's count, "about 40 item and consumable spells", was wrong. | fixed in `aeb02997`: of the dataset's spells, 66 lost `EffectBonusCoefficient` 1, over **77** effect rows (the finding said 78; both builds' cached SpellEffect tables and the committed diff give 77). Twist of Light 1310735 gained 1: its effect became an aura 108. Across the whole client, 875 spells and 1,043 rows lost it. None of the rows is a damage effect, so nothing moves |
| MC-8 | low | pre-existing | `priest.md:12` still said "1,600 mana" for Dark Sacrifice. | fixed in `66641cb0`: it now reads "1,600 mana plus your Spirit". **Waived:** the "Dark Sacrifice when missing" threshold stays at 1,600. A fixed default can't follow the setup's Spirit (about 250 at the defaults). The mana comes in 5 ticks over 15 s while the priest keeps spending, so using it at 1,600 missing wastes little or none of it. Changing the default would also move every Undead priest's followed defaults for a D27 tuning question |

Checks: lint, typecheck, `npm test` and `npm run scrape:check` (every dataset matches, 0 requests).
The remaining unit failures are the ones `build-70009` already has for the slices still to come:
paladin data, goldens and APL, the warrior's Slam, Overpower and Sunder, the bear's Mangle, and
client.test's Righteous Fury and Sunder. The casters' own failures are gone. The engine benchmark
fails only under the machine's load (a load average of 36); timeouts under that load passed when
their files ran alone. E2E, all green (129 tests, `E2E_PORT` 4793): mage, shaman, elemental,
balance, priest, warlock, buffs-for-your-class, buffs-one-of-a-kind, setup-fight-buffs,
protection-rotation (warrior), app (Fury), rogue, hunter, feral-cat and retribution. Screen: the
Buffs tab with the new potion at 1280 px light and 390 px dark: the row wraps cleanly, with no
horizontal scroll.

Verdict: MC-3 changed what the engine runs (a new shared consumable), so a verification pass by a
fresh reviewer is due, scoped to `10e2c4d8`..`4efecf2a`.
