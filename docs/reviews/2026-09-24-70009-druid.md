# The 70009 druid slice (2026-09-24)

The druid slice for build 1.60.1.70009 (Mangle is Primal Bite, Primal Fury is Blood Frenzy,
Lacerate's "high amount of threat" at 206 + 0.05 × AP) was merged onto `build-70009` in `15eca91b`
and had a combined logic and UX review (D27's 90/10 mode) by a fresh reviewer. This log records the
review's findings (DR2) and the fix round that answered them, on the integrated branch.

The headlines the fix round measured on the integrated branch (`scripts/tune/rotation.mjs`'s engine
bundle, each tank's default setup, seed 31101, 20,000 fights): the warrior 1,009.55 TPS, the
paladin 746.97, the bear 1,134.15 (558.62 DPS).

## Findings

| # | Severity | Origin | Finding | Disposition |
| --- | --- | --- | --- | --- |
| DR2-1 | medium | introduced | druid.md §6.3's plausibility note still measured the bear against the officers' 800–900 TPS, which D29 withdrew (no numeric benchmark for tanks). | fixed in `f21bccd4`: the note states the three tanks' 1.60.1.70009 headlines as observations (warrior 1,009, paladin 746, bear 1,134 TPS, seed 31101, 20,000 fights), the reversal (the warrior led on 1.60.1.69913, 1,241 against 1,115; Sunder Armor's new threat took it below the bear, now 12% ahead), Maul at 59% of the bear's threat and what that rests on (Maul's ×1.75 [?], Savage Fury's reach, the rage models), and points to T6's open in-game tests. T6 itself is the paladin slice's and isn't changed here |
| DR2-2 | low | introduced | Lacerate's attack power basis: the druid side gave about 271 at the pull's 1,296 AP, where the warrior's WR-2 uses the AP when the ability lands. | confirmed resolved by the merge (`15eca91b`): druid.md §4.3, §6.3, the threat table and Q15, threat.md's wording table, bear section and open question 4, and `bear-abilities.ts`'s comment all give about **274** at the default bear's ~1,355 AP when its Lacerates land (206 + 0.05 × 1,355 = 273.75), with 1,296 at the pull beside it. No change needed |
| DR2-3 | low | introduced | Primal Bite's four ids: three of them (1238069, 1238070, 1238073) left `spells.json` on 1.60.1.70009, because the docs cited them after the name "Mangle", which the client no longer uses, and nothing said so. | the docs half was already fixed by the merge: druid.md §4.2 lists all four ids, (407995, 1238069, 1238070, 1238073), on one line, and `spells.json` has them. Fixed in `14ce9ff2` so it can't recur silently: the client scraper warns on every spell the docs cited in the `spells.json` it replaces (or checks against) that the new generation no longer extracts, with the previous build and the reason (the client's new name, gone from the client, or no longer cited); `droppedDocCitations` in `lib/docrefs.mjs`, zero npm dependencies. Replaying 1.60.1.70009 over 1.60.1.69913's committed data with the docs as they were before the merge names the three ranks ("the client now calls it \"Primal Bite\"") and Adaptation 1253389, which left the client (the docs cite it as 1.60.1.69913's; nothing the sim models reads it). [client.md](../data/client.md#what-the-docs-decide) says so. Test: `docrefs.test.mjs` ("droppedDocCitations") |
| DR2-4 | — | introduced | A merge note: the merge's conflict resolutions (threat.md's Lacerate text with the warrior's Shield Slam and WR-2 wording, one AP basis everywhere, druid.md §4.2's four ids on one line, `spells.json` regenerated from the cache) should be on record. | handled in the merge commit `15eca91b`, whose message records each resolution; `scrape:check` passed on it with zero requests, and no golden moved |

## Checks

Lint and typecheck clean; `npm test` (3,016 tests) passes; `npm run scrape:check` matches every
dataset with zero requests, and the new warning names nothing on this build (every spell the docs
cite is still extracted). E2E on port 4927: follow-defaults, setup-talents, share-links, notices,
feral-bear and elemental, 65 passed.

The fix round changed docs and the scraper's warnings, not engine logic, so by D27 it needs no
verification pass; DR2-3's scraper change gets a quick fresh check of its commit.
